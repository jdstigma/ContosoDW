# Database files — built locally, never committed
*.db-shm
*.db-wal
*.db
*.duckdb
*.duckdb.wal

# Python
__pycache__/
*.pyc

# Generated profile reports (kept locally, not committed)
profiling/reports/*.txt

# Power BI exports (large Parquet files, built locally)
exports/

# Step timing cache (local state for setup.sh progress estimates)
.step_timings

# OS
.DS_Store
Thumbs.db
