name: contoso
version: '1.0.0'
config-version: 2

profile: contoso

model-paths:  ["models"]
test-paths:   ["tests"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  contoso:
    staging:
      +materialized: view
    marts:
      +materialized: table
